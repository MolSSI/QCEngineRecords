name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Required depends
  - qcelemental
  - pydantic >= 0.20

    # Testing
  - pytest
  - pytest-cov
  - codecov
