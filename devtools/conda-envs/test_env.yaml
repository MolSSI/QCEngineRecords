name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Required depends
  - qcelemental
  - pydantic>=1.8.2

    # Testing
  - pytest
  - pytest-cov
  - codecov
